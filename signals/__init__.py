"""Detection signal modules for Provenance Guard.

Each signal returns a float in [0.0, 1.0] representing the likelihood that
the submitted text is AI-generated (0.0 = likely human, 1.0 = likely AI).
"""
